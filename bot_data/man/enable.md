Enable a command or subcommand that has been disabled on the Guild. Limited to Guild administrators.

Arguments:
* `command`: The command or subcommand to enable.

Examples:
* `%enable echo`
* `%enable role add`
