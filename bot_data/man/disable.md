Disable a command or subcommand for the Guild. Can only be used by Guild administrators. Commands can be re-enabled by the `%enable` command.

The `%enable` command cannot be disabled, but the `%disable` command can.

Arguments:
* `name`: The name of the command or subcommand to disable. Supports aliases.

Examples:
* `%disable echo`
* `%disable channel list`
